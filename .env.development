# RíoClaro Development Environment Configuration
# This file is for development/testing purposes

# Environment
DJANGO_ENVIRONMENT=development
NODE_ENV=development

# Security Settings
SECRET_KEY=django-dev-key-not-for-production-use-only
DEBUG=True

# Database Configuration (Development - SQLite for simplicity)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# For Docker MySQL Development
# DB_ENGINE=django.db.backends.mysql
# DB_NAME=rioclaro_dev
# DB_USER=rioclaro_user
# DB_PASSWORD=dev_password
# DB_HOST=mysql
# DB_PORT=3306

# Allowed Hosts
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# CORS Configuration (Permissive for development)
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOW_CREDENTIALS=True
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:3000

# Frontend Configuration
VITE_API_URL=http://localhost:8000
FRONTEND_URL=http://localhost:5173

# Email Configuration (Development - Console backend)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USE_TLS=False
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=desarrollo@rioclaro.local

# Redis Configuration (Optional for development)
REDIS_URL=redis://localhost:6379/1
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Logging Level
LOG_LEVEL=DEBUG

# Data Source for Development
DATA_SOURCE=SIMULATOR

# Session Configuration (Development)
SESSION_TIMEOUT_MINUTES=1440  # 24 hours
SESSION_COOKIE_AGE=86400      # 24 hours

# Password Policy (Relaxed for development)
PASSWORD_MIN_LENGTH=6
PASSWORD_REQUIRE_UPPERCASE=False
PASSWORD_REQUIRE_LOWERCASE=False
PASSWORD_REQUIRE_NUMBERS=False
PASSWORD_REQUIRE_SYMBOLS=False
PASSWORD_HISTORY_COUNT=0

# Backup Configuration
BACKUP_RETENTION_DAYS=7
BACKUP_STORAGE_PATH=./backups

# Arduino Simulator
SIMULATION_INTERVAL=30

# Admin Configuration
ADMIN_URL=admin/

# Security Headers (Disabled for development)
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0