// Tipos específicos para datos de charts (usando unknown por compatibilidad)
export type ChartDataItem = Record<string, unknown>;

// Tipo específico para arrays de datos de charts
export type ChartDataArray = ChartDataItem[];